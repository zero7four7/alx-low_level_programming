#include "main.h"

/**
 * _strlen - a function that returns the length of a string
 * @s: char input
 *
 * Return: value is 1
 */

int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{

	}
	return (i);
}
